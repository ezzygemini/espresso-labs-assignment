import { deleteAgent } from "./mutation/deleteAgent";
import { editAgent } from "./mutation/editAgent";
import { addAgent } from "./mutation/addAgent";

export const Mutation = {
  addAgent,
  editAgent,
  deleteAgent,
};
